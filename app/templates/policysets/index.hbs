<div class="col-md-12">
    <h2>Policy Sets list</h2>
    <div class="row">
        <div class="col-md-12">
            {{#link-to "policysets.new" class="btn btn-default"}}<span class="glyphicon glyphicon-plus"></span> Add Policy Set{{/link-to}}
        </div>
    </div>
    {{#if model.length}}
        <table class="table table-hover">
            <thead>
            <tr>
                <th>#</th>
                <th>Description</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {{#each model as |policySet|}}
                <tr>
                    <td>{{policySet.id}}</td>
                    <td>{{policySet.description}}</td>
                    <td>{{#link-to "policysets.edit" policySet class="glyphicon glyphicon-pencil"}}{{/link-to}}</td>
                </tr>
            {{/each}}
            </tbody>
        </table>
    {{/if}}
</div>